apiVersion: v1
kind: Service
metadata:
  name: {{ include "nsp-k8s-operator-helm.fullname" . }}
  labels:
    {{- include "nsp-k8s-operator-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nsp-k8s-operator-helm.selectorLabels" . | nindent 4 }}